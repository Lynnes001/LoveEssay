## No Requirement Changes

This change corrects prompt implementation details only. No spec-level behavior changes — the finetune stage's observable contract (input: draft + session context; output: styled essay) is unchanged.
